import React from "react";
import { Navbar } from "../Navebare/Navbar";

class Header extends React.Component {
  render() {
    return(
      <header>
       <Navbar/>
      </header>
    );
  }
}

export default Header;